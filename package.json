{
  "name": "fayda-id-generator",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "dev:api": "python -m uvicorn main:app --reload --reload-dir backend --host 127.0.0.1 --port 8765 --app-dir backend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w frontend",
    "lint": "npm run lint -w frontend"
  }
}
